<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/value_64"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Текст названия региона -->
    <TextView
        android:id="@+id/area_text"
        android:layout_width="@dimen/zero_dp"
        android:layout_height="wrap_content"
        android:paddingEnd="@dimen/value_8"
        android:textColor="@color/region_item_text_color"
        android:letterSpacing="0"
        android:fontFamily="@font/ys_display_regular"
        android:textSize="@dimen/text_size_16"
        android:textAllCaps="false"
        android:maxLines="2"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/area_forward_icon"/>

    <!-- Иконка "Вперед. Регион" -->
    <ImageView
        android:id="@+id/area_forward_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_forward"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/area_text"
        app:layout_constraintTop_toTopOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
